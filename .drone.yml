---                                                                                                                                  
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: testing the exec runner
    commands:
    - echo "yay i'm working!"
